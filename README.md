# BackpackTf JS
NodeJS Based API collection of [backPack.tf](backpack.tf).

## API Methods and usage
#### Index
    `index()`

#### getCurrencies
    ```
    getCurrencies(raw = 0)
    ```

    raw = 1
    raw = 2

#### users
    ``